Puzzle solved. Do this before advancing:

1. memory_note useful lessons that carry across puzzles:
   • Red herrings that fooled you → key="red_herring_WORD"
   • Category shapes that recurred → key="pattern_TYPE"
   • Words with tricky double meanings → key="tricky_WORD"
   Only save what would actually help a future puzzle. Skip obvious observations.

2. Clean up puzzle-specific memory — these notes are now stale:
   • memory_delete every solved_N key (solved_1, solved_2, solved_3, solved_4).
   • memory_delete every hunch_*, partial_*, oneaway_*, ambiguous_*, ruled_out_*, candidate_* key from this puzzle.
   Keeping these will pollute the next game. Delete them now.

3. Call memory_list to confirm the cleanup — only cross-puzzle lessons should remain.

4. Call next_game to advance — then STOP. Do not start guessing the new board here.
   The next puzzle is a fresh start; it begins on the next turn, not now.
