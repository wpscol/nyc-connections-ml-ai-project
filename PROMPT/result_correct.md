Correct group — settled, never re-analyse it.

1. memory_note(key="solved_N", content="WORD1,WORD2,WORD3,WORD4=TITLE") now — so a restart replays it for free.
2. Next:
   • Only 4 words left → submit those exact 4 words immediately. Do NOT analyse — the last group is forced.
   • Otherwise → get_state and probe the next group.
